# AET Platform

Веб-платформа для работы школьного персонала с детьми и молодыми людьми с аутизмом (возраст 6–25 лет). Платформа построена на основе AET (Autism Education Trust) Framework и предоставляет инструменты для ежедневной работы учителей и терапевтов.

## 🚀 Быстрый старт

### Требования

- **Java 17+** (JDK)
- **Maven 3.8+**
- **Node.js 18+**
- **npm**

### Запуск проекта

> **Примечание:** В режиме разработки frontend и backend запускаются на разных портах для удобства. В production (Docker) они работают на одном хосте через nginx proxy.

#### 1. Запуск Backend

```bash
cd backend
mvn spring-boot:run
```

Backend запустится на `http://localhost:8080/api/v1`

При первом запуске автоматически создаются тестовые пользователи и студенты.

#### 2. Запуск Frontend

В новом терминале:

```bash
cd frontend
npm install  # только первый раз
npm run dev
```

Frontend запустится на `http://localhost:3001`

> **Примечание:** Vite dev server автоматически проксирует запросы `/api/v1/*` к backend на `localhost:8080`, поэтому frontend может обращаться к API через относительные пути.

#### 3. Вход в систему

Откройте `http://localhost:3001` в браузере и используйте тестовые аккаунты:

| Роль | Email | Password |
|------|-------|----------|
| Администратор | admin@school.com | admin123 |
| Учитель | teacher@school.com | teacher123 |
| Терапевт | therapist@school.com | therapist123 |

## 📁 Структура проекта

```
Al_karamah/
├── backend/                 # Spring Boot приложение
│   ├── src/main/java/com/school/aet/
│   │   ├── auth/           # Аутентификация и JWT
│   │   ├── user/           # Управление пользователями
│   │   ├── student/        # Профили студентов и AET профили
│   │   ├── group/          # Управление группами
│   │   ├── session/        # Планирование сессий и календарь
│   │   ├── resource/       # Управление ресурсами
│   │   ├── aet/            # AET framework (домены и statements)
│   │   └── config/         # Конфигурация
│   └── src/main/resources/
│       └── application.yml
│
└── frontend/               # React TypeScript приложение
    ├── src/
    │   ├── api/           # API клиенты
    │   ├── components/    # React компоненты
    │   ├── pages/         # Страницы
    │   ├── store/         # Zustand state management
    │   └── types/        # TypeScript типы
    └── package.json
```

## 🎯 Основные возможности

### 1. Календарь сессий
- Недельный/месячный вид всех сессий
- Цветовая кодировка по AET доменам
- Быстрый доступ к деталям сессии

### 2. Управление группами
- Создание групп с возрастными диапазонами
- Проверка совместимости при добавлении студентов
- Визуальная оценка совместимости

### 3. Профили студентов
- Информация о студентах и заметки
- AET профиль с матрицей доменов и уровней
- Поиск и фильтрация

### 4. Планирование сессий
- Создание сессий, связанных с группами
- Тегирование AET statements
- Предложения ресурсов на основе AET фокуса

## 🛠 Технологии

### Backend
- **Spring Boot 3.2.0** + Java 17
- **Spring Security** + JWT аутентификация
- **Spring Data JPA** + H2 Database
- **Maven**

### Frontend
- **React 18** + TypeScript
- **Vite** - сборщик
- **React Router** - маршрутизация
- **Zustand** - управление состоянием
- **React Big Calendar** - календарь
- **Axios** - HTTP клиент

## 📡 API Endpoints

### Аутентификация
- `POST /api/v1/auth/login` - Вход в систему
- `GET /api/v1/auth/me` - Текущий пользователь

### Студенты
- `GET /api/v1/students` - Список студентов (с фильтрами)
- `GET /api/v1/students/{id}` - Детали студента
- `POST /api/v1/students` - Создать студента
- `PUT /api/v1/students/{id}` - Обновить студента

### Группы
- `GET /api/v1/groups` - Список групп
- `GET /api/v1/groups/{id}` - Детали группы
- `POST /api/v1/groups` - Создать группу
- `POST /api/v1/groups/{id}/students` - Добавить студентов в группу
- `GET /api/v1/groups/{id}/compatibility` - Проверка совместимости

### Сессии
- `GET /api/v1/sessions/calendar` - Сессии для календаря
- `GET /api/v1/sessions/{id}` - Детали сессии
- `POST /api/v1/sessions` - Создать сессию
- `POST /api/v1/sessions/{id}/resources/suggestions` - Предложения ресурсов

### AET Framework
- `GET /api/v1/aet/domains` - Список AET доменов
- `GET /api/v1/aet/domains/{id}/statements` - Statements по домену
- `GET /api/v1/aet/statements` - Список statements (с фильтрами)

### Ресурсы
- `GET /api/v1/resources` - Список ресурсов (с фильтрами)
- `GET /api/v1/resources/{id}` - Детали ресурса

## 🐳 Docker

Для запуска через Docker:

```bash
# Production режим
docker-compose up -d --build

# Development режим
docker-compose -f docker-compose.dev.yml up -d --build

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

### Архитектура в Docker

**Production режим** (`docker-compose.yml`):
- Frontend и Backend работают на **одном хосте** через nginx reverse proxy
- Приложение доступно по адресу `http://localhost:3000`
- Nginx проксирует запросы `/api/v1/*` к backend контейнеру
- Backend не экспортируется наружу, доступен только через nginx

**Development режим** (`docker-compose.dev.yml`):
- Frontend на `http://localhost:3000` (Vite dev server в Docker)
- Backend на `http://localhost:8080` (отдельно для удобства разработки)

> **Примечание:** При локальной разработке (без Docker) frontend запускается на порту 3001, а в Docker dev режиме — на порту 3000.

## 🔧 Разработка

### Backend

```bash
cd backend
mvn clean compile      # Компиляция
mvn spring-boot:run    # Запуск
mvn test              # Тесты
```

### Frontend

```bash
cd frontend
npm install           # Установка зависимостей
npm run dev          # Запуск dev сервера
npm run build        # Сборка для production
```

## 📝 Примечания

- Backend использует H2 in-memory базу данных для разработки (данные сбрасываются при перезапуске)
- JWT токены хранятся в localStorage (frontend)
- CORS настроен для `localhost:3000`, `localhost:3001` и `localhost:5173`
- Все даты/время используют ISO 8601 формат
- Тестовые пользователи и студенты создаются автоматически при первом запуске

## 📄 Лицензия

Проект для образовательного/внутреннего использования.
